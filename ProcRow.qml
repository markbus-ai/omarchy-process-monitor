import QtQuick

// NOTE: superseded by the static 3-level nesting inside ProcessMonitor.qml
// (QML forbids a file from instantiating itself). Kept as an empty stub so
// old references fail loudly instead of silently. Safe to delete.
Item {
}
